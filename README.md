###Dependencies
- Use Ruby 2.1.3
- Install bundler
    
        [host]$ gem install bundler

###Setup
    [host]$ git clone git@github.com:bonniecpk/handy-rpn-calculator.git
    [host]$ bundle

###To run
    [host]$ ruby app.rb

###To run tests
    [host]$ rspec
